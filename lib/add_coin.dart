// lib/add_coin.dart
import 'package:flutter/material.dart';

class AddCoinScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Здесь должен быть интерфейс для добавления монет
    return Scaffold(
      appBar: AppBar(
        title: Text('Добавить монеты'),
      ),
      body: Center(
        child: Text('Интерфейс для добавления монет'),
      ),
    );
  }
}
